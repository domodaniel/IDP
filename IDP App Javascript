// author: Daniel Uyematsu
var skip_lines = function(a){
    for(i = 0; i < a; i++){
        console.log("");
    }
};
var printIngredients = function(ingredients){
    for(var i in ingredients) {
        console.log("-" + ingredients[i]);
    }
};
function foodMaker(name,calories,fat){
    this.name = name;
    this.calories = calories;
    food.push(this);
    this.fat = fat;
}
function printInfo(object){
    console.log(object.name);
    console.log("-----------");
    console.log("Calories: " + object.calories);
    console.log("Fat: " + object.fat + "g");
}
console.log("Welcome to HEAL\'s App!");
console.log("We are so glad that you could veiw our website and download this informative program.");
console.log("We have information about McDonald\'s food available.");
skip_lines(1);
// database here below
var food = [];
    //  examples!!!!!!!!!!!!
// var burger = new foodMaker("burger",1234,34);
// var fatBurger = new foodMaker("Bacon DoubleStack TripleCheese Advocado OnionSauce Burger", 123456789,67);
var BaconClubhouseBurger = new foodMaker("Bacon Clubhouse Burger", 720, 40);
var BaconClubhouseGrilledChickenSandwich = new foodMaker("Bacon Clubhouse Grilled Chicken Sandwich",610,26);
var McChicken = new foodMaker("McChicken",370,17);
// database above

// asking user what he or she wants to view
b = true;
while(b){
    a = prompt("What would you like to view, McDonald\'s Nutrition Facts or healthy recipes?(McDonald\'s / recipes)");
    while(a!="McDonald\'s" && a != "recipes"){
        a = prompt("What would you like to view, McDonald\'s Nutrition Facts or healthy recipes?(McDonald\'s / recipes)");
        if(a!="McDonald\'s" && a != "recipes"){
            console.log("I didn't get that. Please type \"McDonald\'s\" or \"recipes\"")
        } else{
            c = confirm("You have chosen to look at " + a + ". Are you sure you want to do this?");
            if(c){
                b  = false;
            }
            else{
                b = true;
            }
        }
    }
}

skip_lines(1);
printInfo(food[0]);
skip_lines(2);
printInfo(food[1]);
skip_lines(2);
printInfo(food[2]);
